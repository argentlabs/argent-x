import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs';

import { colors } from "@argent-x/ui/src/theme/theme"

import { groupedColors } from './groupedColors'

<Meta
  title="theme/Colors"
  parameters={{
    viewport: {
      defaultViewport: "reset",
    },
  }}
/>

<ColorPalette>
  {Object.entries(groupedColors).map(([title, colors]) => {
    return <ColorItem key={title} title={title} colors={colors} />
  })}
</ColorPalette>
