import { Center, chakra } from "@chakra-ui/react"

export const ContentBox = chakra(Center, {
  baseStyle: {
    backgroundColor: "#333332",
    rounded: "base",
    color: "#666",
    minHeight: 64,
  },
})
